# Copy to mobile/.env

# SaveLoom API — must be reachable from your phone's browser during Google sign-in.
#
# iOS simulator / Android emulator:
EXPO_PUBLIC_API_URL=http://localhost:3000
#
# Physical device: LAN often blocked by firewall. Use a tunnel instead:
#   Terminal A: npm run dev
#   Terminal B: npx cloudflared tunnel --url http://localhost:3000
#   Copy the https://....trycloudflare.com URL below:
# EXPO_PUBLIC_API_URL=https://YOUR-SUBDOMAIN.trycloudflare.com

# Skip login in development (mock user + sample dashboard data).
# Set to false when Google sign-in is working again.
# EXPO_PUBLIC_DEV_BYPASS_AUTH=false

# Optional: use your real SaveLoom account instead of sample data.
# 1. Sign in at http://localhost:3000 in your browser
# 2. Open http://localhost:3000/dev/mobile-token
# 3. Paste the line below into mobile/.env and restart Expo
# EXPO_PUBLIC_DEV_ACCESS_TOKEN=your-jwt-here
